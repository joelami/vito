[pytest]
testpaths = tests
# The pandas groupby.apply FutureWarning that shows up throughout this
# codebase's real runs (sports/nhl/features.py etc.) is pre-existing noise
# unrelated to what these tests check -- silenced here so a real failure
# in test output is never lost in fifty warning lines.
filterwarnings =
    ignore::FutureWarning
